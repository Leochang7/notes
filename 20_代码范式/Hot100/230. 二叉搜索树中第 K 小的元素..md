#二叉树 
#难度：Medium
#独立完成：是
### 解法一：中序遍历
线索二叉树的中序遍历即为升序排列，用一个列表存放中序遍历的值，最后取出索引为k-1的元素即为第k小元素
```java
class Solution {
    public int kthSmallest(TreeNode root, int k) {
        List<Integer> list = new ArrayList<>();
        inOrder(root,list);
        return list.get(k-1);
    }

    public void inOrder(TreeNode node,List list){
        if(node == null){
            return;
        }
        inOrder(node.left,list);
        list.add(node.val);
        inOrder(node.right,list);
    }
}
```